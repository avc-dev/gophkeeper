// Package auth реализует клиентский сервис аутентификации.
//
// Отвечает за регистрацию и вход пользователя через gRPC,
// а также за управление локальными учётными данными (JWT, kdf_salt, last_sync_at).
//
// Мастер-ключ никогда не сохраняется на диск: он деривируется заново
// при каждом вызове [Service.DeriveMasterKey] из хранящегося kdf_salt и пароля.
//
// Sentinel-ошибка [ErrNotLoggedIn] сигнализирует об отсутствии сохранённых учётных данных.
package auth
